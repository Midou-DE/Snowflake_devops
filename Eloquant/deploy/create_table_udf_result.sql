USE DATABASE TEST_ADK;
USE SCHEMA ELOQUANT;

CREATE OR ALTER TABLE udf_results (
  id INTEGER AUTOINCREMENT,
  num1 NUMBER,
  num2 NUMBER,
  result NUMBER,
  run_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);